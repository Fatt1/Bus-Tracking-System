namespace TrackingBusSystem.Shared.Constants
{
    // Dùng cho chiều đi và chiều về
    public enum TripDirection
    {
        Outbound, // Chiều đi (ví dụ: đến trường)
        Inbound   // Chiều về (ví dụ: từ trường về nhà)
    }
}
